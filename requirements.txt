# ============================================================================
# PRODUCTION REQUIREMENTS — Collectors League Scan API
# Cleaned up: removed packages that are in requirements but never imported
# in main.py (psycopg2, SQLAlchemy, alembic, redis, python-jose, passlib,
# openai SDK). Added scipy (actually used). Pinned more tightly.
# ============================================================================

# Core FastAPI & Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# HTTP Client (used directly — OpenAI is called via raw HTTP, not SDK)
httpx==0.28.1

# Environment Variables
python-dotenv==1.0.1

# NOTE: The openai Python SDK is NOT imported anywhere in main.py.
# All OpenAI calls are raw httpx POST requests to api.openai.com.
# Removing it saves ~60MB of cold-start memory on Render's free tier.
# If you ever switch to the SDK, add: openai>=2.32.0

# Anthropic SDK — used by /api/ai-grade and _claude_market_lookup
anthropic>=0.25.0

# Web Scraping
beautifulsoup4==4.12.3
lxml==5.2.2

# QR Codes
qrcode[pil]==7.4.2

# Image Processing
Pillow>=11.0.0

# Numerical / Scientific (numpy + scipy both used in fingerprint + market trend code)
numpy==1.26.3
scipy==1.13.1

# ============================================================================
# REMOVED — these were in requirements but are NEVER imported in main.py.
# The app uses SQLite (stdlib), not PostgreSQL. No Redis, no JWT auth layer.
# Keeping them added ~180MB to the Render slug and slowed cold starts.
# ============================================================================
# psycopg2-binary    — PostgreSQL driver (app uses sqlite3)
# sqlalchemy         — ORM (app uses raw sqlite3 calls)
# alembic            — DB migrations (app uses CREATE TABLE IF NOT EXISTS)
# redis              — Caching (app uses in-process dicts + SQLite)
# python-jose        — JWT tokens (no JWT auth in main.py)
# passlib[bcrypt]    — Password hashing (no password hashing in main.py)

# ============================================================================
# OPTIONAL MONITORING (uncomment when ready)
# ============================================================================
# prometheus-client==0.19.0
# python-json-logger==2.0.7
# sentry-sdk==1.40.0

# ============================================================================
# OPTIONAL RATE LIMITING
# ============================================================================
# slowapi==0.1.9

# ============================================================================
# TESTING (development only)
# ============================================================================
# pytest==7.4.3
# pytest-asyncio==0.21.1

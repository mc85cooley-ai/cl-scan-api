# CL Scan API — production dependencies
# Only packages actually imported by main.py

# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic>=2.0.0

# HTTP client
httpx==0.28.1

# AI / vision
openai>=2.29.0

# Image processing
Pillow==11.1.0

# QR codes
qrcode[pil]==7.4.2

# Numerical (optional — app gracefully degrades if unavailable)
numpy==1.26.3
scipy==1.13.1
